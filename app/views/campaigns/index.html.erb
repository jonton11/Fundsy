<h1>All Campaigns</h1>

<h2>Recent Campaigns</h2>

<% @campaigns.each do |campaign| %>
  <div class="well">
    <h3>
      <%= link_to campaign.title.capitalize, campaign %>
    </h3>
    <hr>
  </div>
<% end %>
